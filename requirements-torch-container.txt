# To not install the gpu depencendcies because we don't use then
--index-url https://download.pytorch.org/whl/cpu

--extra-index-url https://pypi.org/simple

torch
torchvision
torchaudio

# Dependencies
fastapi==0.109.0
uvicorn==0.27.0
python-multipart==0.0.6
ultralytics==8.3.25
numpy==1.26.3
Pillow==10.2.0